version: '3'

services:
  app:
    build: .
    container_name: nextjs-app
    ports:
      - "3000:3000"

  nginx:
    image: nginx:latest
    container_name: nginx-server
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app